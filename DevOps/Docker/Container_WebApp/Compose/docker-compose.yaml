version: "3"
services:
  app:
    image: multistage-app
    ports:
      - "5000"
    restart: always


  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf


